// export function verifyRulesOnClaims(claims: HTMLElement[]) {
//   const claimsStatements = claims.map(
//     (claim) => claim.lastChild,
//   ) as HTMLDivElement[];
//   return groupMageStatements(claimsStatements);
// }

// function groupMageStatements(divs: HTMLDivElement[]) {
//   const [mage1, mage2, mage3] = [
//     divs.slice(0, 2).map((div) => div.id),
//     divs.slice(2, 4).map((div) => div.id),
//     divs.slice(4, 6).map((div) => div.id),
//   ];
//   return [mage1, mage2, mage3];
// }
